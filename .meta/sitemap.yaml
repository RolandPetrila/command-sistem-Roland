# Sitemap — Roland Command Center
# Generated: 2026-05-21 (initial manual)
# AUTO-REGENERATED by .claude/scripts/indexer.ps1 (E5)
# Depth: 3 | Excludes: node_modules, .git, .venv, __pycache__, dist, .archive (gitignored content), .ruff_cache

meta:
  schema_version: "1.0"
  generated_at: "2026-05-21T19:00:00+03:00"
  generator: "manual (E2 initial)"
  max_depth: 3
  excludes:
    [
      "node_modules",
      ".git",
      ".venv",
      "__pycache__",
      "dist",
      ".ruff_cache",
      ".playwright-mcp",
    ]

root:
  files:
    - {
        name: "CLAUDE.md",
        purpose: "Regulament proiect (E6 — va fi slim <200 linii)",
      }
    - {
        name: "PLAN_RESTRUCTURARE_2026-05-21.md",
        purpose: "Plan executie restructurare (temporar, mut in .archive/ dupa E7.5)",
      }
    - {
        name: "start.py",
        purpose: "Launcher unic Python (dev/prod/tunnel/stop)",
      }
    - {
        name: "launch_roland.vbs",
        purpose: "Shim auto-start invisible (Task Scheduler)",
      }
    - { name: ".gitignore", purpose: "Git ignore + token exclude" }
    - { name: ".gitattributes", purpose: "Git CRLF normalization" }

  target_after_E6:
    - "README.md (new)"
    - "RUNBOOK.md (new)"
    - "CLAUDE.md (slim)"
    - "start.py"
    - "launch_roland.vbs"
    - ".gitignore + .gitattributes"

folders:
  - path: "backend/"
    purpose: "FastAPI Python 3.13 + SQLite (aiosqlite)"
    children:
      - "app/ — main.py, config.py, core/, db/, module_discovery.py, api/routes_*.py"
      - "modules/ — 14 module auto-discovered (ai, automations, calculator, converter, filemanager, integrations, invoice, itp, quick_tools, quick_tools_extra, reports, timetracking, translator, vault)"
      - "migrations/ — 25 SQL files (000-025)"
      - "tests/ — pytest 86 tests"
      - "uploads/ — runtime files (gitignored)"
      - "data/ — SQLite + backups (gitignored)"

  - path: "frontend/"
    purpose: "React 18 + Vite + Tailwind + PWA"
    children:
      - "src/ — App.jsx, api/client.js, modules/manifest.js, components/, pages/ (26 pages), hooks/"
      - "public/ — static assets + PWA manifest"
      - "vite.config.js — PWA autoUpdate config"
      - "dist/ — production build (gitignored)"
      - "node_modules/ — deps (gitignored)"

  - path: ".meta/"
    purpose: "Single Source of Truth structurat (this folder, NEW Faza 34 restructurare)"
    children:
      - "profile.yaml — variant + composition (A+R)"
      - "status.yaml — stare curenta proiect"
      - "decisions.yaml — index decizii arhitecturale (13 entries)"
      - "sitemap.yaml — acest fisier"

  - path: "docs/"
    purpose: "Narrative arhitectural (NEW Faza 34 restructurare)"
    children:
      - "README.md — navigare docs/"
      - "resume.md — punct curent pentru reluare sesiune"
      - "todo.md — actiuni manuale user + cod"
      - "handovers/ — documente transfer intre sesiuni"
      - "audit-externe/ — audit-uri Gemini etc."
      - "plan.md (planned E3) — plan curent <500 linii"
      - "archive/faze_anterioare.md (planned E3) — faze 0-32 detalii"

  - path: "99_Roland_Work_Place/"
    purpose: "USER_WORKSPACE — zona personala user (notite, audit, capturi)"
    children:
      - "0.0_PLAN_EXTINDERE_COMPLET.md (split in E3 -> docs/)"
      - "GHID_TESTARE.md"
      - "ROADMAP_IMPLEMENTARE.md"
      - "CATALOG_API_GRATUITE.md"
      - "GHID_ACCES_REMOTE.md"
      - "Calculator_pret/ — PDF-uri test calibrare + test-scripts/"
      - "RECOMANDARI_IMBUNATATIRI.md"
      - "CLOUDFLARE_TUNNEL_SETUP.md"
      - "archive/ — documente vechi user"
      - "2026-03-21_deep_research/, 2026-03-21_deep_research_v2/ — istorice"

  - path: ".workspace/ (planned E4)"
    purpose: "Zona AI work — drafts, investigations, audit-outputs (gitignored)"
    children:
      - "drafts/ — planuri WIP pre-aprobare"
      - "investigations/ — bug analyses, RCA, profiling"
      - "audit-outputs/ — rezultate audit (skill /audit etc.)"

  - path: ".archive/"
    purpose: "Documente si artefacte deprecated (P7 — arhiva over delete)"
    children:
      - "README.md — index continut"
      - "PLAN_EXECUTIE.md — Faza 0-8 deprecated"
      - "SABLOANE_SI_HINTURI_RECOMANDATE.md — Faza 20 deprecated"
      - "update_tracking.py — script vechi tracking"
      - "roland_backend.log, roland_start.log — runtime logs (gitignored)"

  - path: ".claude/"
    purpose: "Sistem Claude Code"
    children:
      - "rules/ — 8 reguli proiect (.md)"
      - "hooks/ — 5 hooks (.sh)"
      - "scripts/ (planned E5) — sync-meta + indexer + root-sweeper"
      - "PROJECT_STATUS.md — auto-update status"
      - "settings.local.json — config (NU edit by AI)"

  - path: "scripts/"
    purpose: "PowerShell + Python automation (Cloudflare tunnel, register tasks)"
    children:
      - "setup_named_tunnel.py, setup_cf_tunnel_api.py — tunnel setup"
      - "auto-start-task.xml — Task Scheduler XML"
      - "_*.ps1 — utility scripts (CF login, restart, status, etc.)"
      - "launch_roland.ps1, check_and_restart.ps1, renew_cert.py"

  - path: "cloudflared/"
    purpose: "Cloudflare Tunnel runtime config"
    children:
      - "config.yml — tunnel UUID + ingress"
      - "tunnel_token.txt — secret (gitignored)"

  - path: "Fisiere_Reper_Tarif/"
    purpose: "26 PDF-uri calibrare calc pret traduceri (120-10820 RON)"
    children:
      - "Pret_Intreg_100la100/ — PDF-uri cu pret real"
      - "_reference_cache.json — features extracted"

  - path: "logs/"
    purpose: "Runtime logs (Faza 34 — gitignored)"
    children:
      - "backend.log, watchdog.log, vite.log"
