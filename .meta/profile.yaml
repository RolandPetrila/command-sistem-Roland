# Profile — Roland Command Center
# Generated: 2026-05-21 (E2 Native Workspace restructure)
# Updates: regenerated by .claude/scripts/sync-meta.ps1 (when status changes)

meta:
  schema_version: "1.0"
  generated_at: "2026-05-21T19:00:00+03:00"
  generator: "manual (initial scaffold)"

project_type:
  primary: A
  composition: [A, R]
  confidence: 1.0

detection:
  evidence:
    - "frontend/package.json: react + vite (Variant A — web full-stack frontend)"
    - "backend/pyproject.toml: fastapi + uvicorn (Variant R — backend pure API)"
    - "backend/migrations/: SQLite migrations system"
    - "backend/modules/*/router.py: FastAPI module auto-discovery pattern"
    - "frontend/src/modules/manifest.js: sidebar nav as single source"
  inferred_stack: "FastAPI Python 3.13 + React 18 + Vite + SQLite (aiosqlite) + Tailwind"
  user_workspace: "99_Roland_Work_Place"

adaptations:
  codebase_folders:
    backend: "backend/"
    frontend: "frontend/"
  assets_folder: "assets/"
  user_workspace_folder: "99_Roland_Work_Place/"
  reference_data_folder: "Fisiere_Reper_Tarif/"
  extra_meta_files:
    - "status.yaml"
    - "decisions.yaml"
    - "sitemap.yaml"
  deferred_meta_files:
    - "modules.yaml"
    - "routes.yaml"
    - "dependencies.yaml"
    - "inbox.yaml"
    - "schemas/"
  extra_scripts:
    - ".claude/scripts/sync-meta.ps1"
    - ".claude/scripts/indexer.ps1"
    - ".claude/scripts/root-sweeper.ps1"
  deferred_scripts:
    - "status-board.ps1"
    - "verify-deps.ps1"
    - "ingest-sources.ps1"
    - "build-docs.ps1"

scale:
  user_count: 1
  user_type: "single owner — Roland (CIP Inspection SRL)"
  deployment: "self-hosted (Tailscale + Cloudflare Tunnel)"
  data_scale: "small (SQLite, no horizontal scaling needed)"
  filters_applied:
    - "P13 Scale lens: skip enterprise patterns"
    - "P14 Tool overhead: skip schemas + MCP guard + status-board (overkill for single-user)"
